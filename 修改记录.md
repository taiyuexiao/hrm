# 修改记录

## 2026-08-11

### 〇、重大坑记录：后端相对数据目录 + SQLite 静默建库导致连错数据库（运维事故复盘）

> ⚠️ **这是一个容易复发且难以察觉的坑，所有本地启动/部署后端的人都应阅读本节。**

#### 事故现象

超级管理员 `33528` 登录后「账号管理」「权限管理」菜单消失（权限为空 `[]`）。同一根因还导致当日早些时候的：

- 老用户被系统当成「首次登录」，强制要求修改初始密码；
- 修改密码看似操作了但登录时报「密码错误」；
- 浏览器密码管理器弹窗造成进一步误解（它只更新浏览器本地保存的密码，与服务器无关）。

#### 根因链条（四个环节缺一不可）

1. **启动位置错误**：以**项目根目录**为工作目录执行 `java -jar backend/target/xxx.jar`，而非先 `cd backend`。
2. **配置使用相对路径**：`application.yml` 中 `app.data-dir: ${DATA_DIR:./data}`，数据库 URL 为 `jdbc:sqlite:./data/hr.db`。相对路径按**启动时的工作目录**解析，于是指向了不存在的 `项目根目录/data/hr.db`。
3. **SQLite 静默建库**：SQLite JDBC 连接不存在的 `.db` 文件时**不报错，直接创建空文件**（与 MySQL 连错库会报错完全不同），日志中无任何异常。
4. **应用自动初始化**：Spring Boot 启动时发现表不存在，自动建表并用 `users.json` 播种初始账号（初始密码、空权限）——假库结构完整、接口全部"正常"，极具迷惑性。

#### 为什么当时没发现

- 登录、改密等 API 均正常返回，无报错；
- 只验证了"接口通不通"，未验证"数据对不对"（如周报数据是否还在、权限是否完整）。

#### 修复措施（已执行）

1. 重启后端，工作目录改为 `backend/`，`./data` 正确指向真实库 `backend/data/hr.db`；
2. 删除误建的 `项目根目录/data/hr.db`；
3. 验证 `33528` 登录成功、22 项权限完整返回。

#### 预防措施（务必遵守 / 可选改进）

- **守则**：本地启动后端必须先 `cd backend` 再执行 `java -jar target/xxx.jar`；或显式指定 `DATA_DIR=backend/data`（绝对路径更稳妥）。
- **判断连错库的信号**：老用户被要求改初始密码、权限为空、历史数据消失——三者出现其一，第一时间检查后端工作目录和 `DATA_DIR`。
- **可选技术改进**（待实施）：
  - 后端启动时自检：若 `hr.db` 不存在或核心表为空，打印醒目警告或拒绝启动；
  - 提供带 `cd backend` 的本地启动脚本，从流程上杜绝。

### 一、首登强制改密页增加退出入口

#### 背景
首次登录（初始密码）会强制进入「修改初始密码」整页。该页面是 App 顶层条件渲染（不在路由内），且没有任何返回/关闭入口。一旦改密环节出错（接口报错、后端宕机等），用户会被困死在该页，刷新也无效（token 已在 localStorage，JWT 带 `needChangePassword` 标记，刷新后仍强制回到改密页），只能手动清浏览器数据。

#### 改动文件

| 文件 | 说明 |
|------|------|
| `src/pages/ChangePasswordPage.tsx` | 新增 `onCancel` 回调属性；卡片右上角新增 ✕ 关闭按钮（`CloseOutlined`，悬停提示「返回登录页」） |
| `src/App.tsx` | 新增 `handleCancelChangePassword`（清除 `auth-token`/`auth-user` 并将 token 状态置空），渲染改密页时传入；安全性不变——用初始密码再次登录仍会回到改密页 |

### 二、密码策略调整：不再拦截全角字符（NIST 800-63B 风格）

#### 背景
原密码规则为白名单制：强制大小写+数字+特殊字符，且只允许半角 `!@#*&`。用户使用中文输入法时容易打出全角字符（如 `！`），被前端拦截且报错不醒目，导致「以为改密成功实际未生效」，后续登录提示密码错误。另外浏览器密码管理器弹窗（选择更新哪个账号）只更新浏览器本地保存的密码，与服务器无关，加剧了误解。

#### 新规则
- 接受任意字符（全角、中文、空格均可），不做全半角转换，输入什么存什么
- 长度 8~64 位（64 上限是 BCrypt 超过 72 字节会静默截断的保护）
- 拒绝 18 个常见弱口令（`12345678`、`password` 等）
- 保留「不能等于初始密码」校验

#### 改动文件

| 文件 | 说明 |
|------|------|
| `src/utils/password.ts` | **新增** 共享密码校验：`validatePassword()` + 弱口令字典 + `PASSWORD_RULE_HINT` 提示文案 |
| `src/pages/ChangePasswordPage.tsx` | 移除旧强密码正则，改用 `validatePassword`；表单校验与提示文案更新 |
| `src/App.tsx` | 改密弹窗同步改用 `validatePassword`，新密码框下增加规则提示 |
| `backend/.../controller/AuthController.java` | 3 处校验（改密、建用户、重置密码）统一替换为 `validatePassword()` 静态方法，删除旧 `PASSWORD_REGEX` |

#### 验证结果
- TypeScript 编译通过；后端重新打包重启（8080 端口）
- API 端到端测试：弱口令/过短密码正确拦截；全角密码 `密码abc123！` 可改可登；账号 `33528` 密码已恢复为 `B@s95594!!` 且 `needChangePassword=false`

### 四、密码变更写入用户行为日志

#### 背景
应需求：用户修改密码需留痕到用户行为日志（`user_action_logs`），便于审计。

#### 实现
- 用户自己改密（`/auth/change-password`）→ 记录 `change_password` 日志（操作人=本人，对象=用户）
- 管理员重置密码（`/auth/users/{username}/reset-password`）→ 记录 `reset_password` 日志（操作人=管理员）
- ⚠️ 日志 details 为 NULL，**不记录任何密码内容**（不明文、不哈希），只记录"谁在何时对谁做了改密/重置"及来源 IP

#### 改动文件

| 文件 | 说明 |
|------|------|
| `backend/.../controller/AuthController.java` | 注入 `UserActionLogService`；改密/重置成功后写日志；新增 `getClientIp` 辅助方法 |
| `src/components/admin/ActionLogPage.tsx` | 行为标签新增：`change_password`「修改密码」(purple)、`reset_password`「重置密码」(volcano)；筛选下拉自动生效 |

#### 验证结果
API 端到端测试：用户改密与管理员重置均正确落库（操作人、对象、描述、IP 齐全，无密码内容）；测试账号已清理。

### 五、截止时间提示与实际截止日期脱钩修复

#### 背景
周报编辑页部门名称下方的提示板「本周提交截止时间：周五 20:00，还剩 X」中，前半段「周五 20:00」是写死的文案，与管理员实际设置的自定义截止时间脱钩（后半段倒计时是动态的）。已截止时的「已截止（上周五 20:00）」同样写死。

#### 实现
- 后端报告详情接口新增 `deadlineTime` 字段：返回**实际生效的截止时间**（自定义截止或默认周五 20:00），作为唯一数据源
- 前端三处接收 detail 的位置同步保存 `deadlineTime`；新增 `formatDeadlineText()` 格式化为「8月14日 周五 20:00」
- 提示板三态文案（未截止/已截止/管理员授权）全部改为动态渲染实际截止时间
- 顺带修复：前端本地兜底逻辑（后端无报告时）原把默认截止算成了**周六** 20:00（多加了 1 天），与后端的周五 20:00 不一致，已对齐

#### 改动文件

| 文件 | 说明 |
|------|------|
| `backend/.../controller/ReportController.java` | detail 响应新增 `deadlineTime` 字段 |
| `src/components/weekly-report-v2/types.ts` | `WeeklyReport` 新增 `deadlineTime?: string` |
| `src/components/weekly-report-v2/WeeklyReportV2.tsx` | 新增 `deadlineTime` 状态、`toLocalIso`/`formatDeadlineText` 辅助函数；三处 detail 处理同步；提示板文案动态化；本地兜底逻辑与后端对齐 |

#### 验证结果
API 验证：`/api/reports/20260814/综合管理部` 返回 `deadlineTime: 2026-08-14T20:00`（默认周五）；自定义截止的报告将返回自定义时间。TypeScript 编译通过，后端已重启。

### 六、导出 Excel 与填写页科室 Tab 按固定部门次序排列

#### 背景
导出周报的部门顺序原先不固定；需求要求按固定次序：项管→架构→需求→综合→治理→信统→信管→机构→开发→平台→测试→研发→智能平台→一部→二部（简称，实际输出全称）。填写页科室 Tab 也按同一次序。

#### 固定次序（全称）
项目管理 → 架构管理 → 需求管理 → 综合管理部 → 数据治理部 → 信息统计部 → 信息管理部 → 机构服务团队 → 数据开发部 → 数据平台部 → 数据测试部 → 研发管理部 → 智能平台部 → 智能应用一部 → 智能应用二部（共 15 个）

#### 改动文件

| 文件 | 说明 |
|------|------|
| `src/components/weekly-report-v2/types.ts` | 新增 `DEPT_ORDER`（固定次序）、`sortByDeptOrder()`（未收录部门排末尾）、`SORTED_DEPTS`；原 `DEPTS` 保持不变（避免影响各处下拉默认值） |
| `src/components/weekly-report-v2/WeeklyReportV2.tsx` | 导出当前周期：按部门次序排序；导出全部周期：先部门次序再按周期稳定排序；科室 Tab 栏改用 `SORTED_DEPTS` |

#### 验证结果
排序逻辑 node 实测输出与需求次序一致；TypeScript 编译通过。纯前端改动，无需重启后端。

---

### 三、超级管理员可在账号管理中查看用户明文密码

#### 背景
用户忘记密码时，管理员无法告知其原密码（BCrypt 单向哈希不可逆），只能重置。应需求支持超管 `33528` 在「账号管理」弹窗中查看用户明文密码，用户改密后同步更新。

#### 实现方案
- `users` 表新增 `password_plain` 列存明文密码（登录校验仍使用原 BCrypt 哈希列，**认证逻辑完全不受影响**）
- 所有密码写入路径同步更新明文：用户改密、管理员建用户、管理员重置密码
- 存量数据回填：哈希与初始密码匹配的用户回填为初始密码（34 个）；已改过密码的存量用户明文不可知，显示「未知」（下次改密/重置后可见）；`33528` 明文已手动补录
- 明文仅通过超管专属接口 `/api/auth/users` 返回（该接口本就走 `checkSuperAdmin` 校验）

#### 改动文件

| 文件 | 说明 |
|------|------|
| `backend/.../entity/User.java` | 新增 `passwordPlain` 字段 |
| `backend/.../dao/UserDao.java` | 新增列的自动迁移（`ALTER TABLE`）、`updatePassword` 支持同时写明文、新增 `updatePasswordPlain`、`save`/`mapRow` 适配 |
| `backend/.../config/InitialPasswordResetRunner.java` | 新增启动时明文回填逻辑（幂等）；初版把回填放在 UserDao 导致 Bean 循环依赖（UserDao→PasswordEncoder→SecurityConfig→JwtFilter→UserService→UserDao），故移至本 Runner |
| `backend/.../controller/AuthController.java` | 改密/建用户/重置密码三处同步写明文；用户列表返回 `passwordPlain` |
| `src/components/user-management/UserManagement.tsx` | 表格新增「密码」列：默认 `******` 掩码，右侧小眼睛图标点击切换显示/隐藏（逐行独立）；明文为 NULL 显示「未知」；弹窗宽度 800→960 |

#### 验证结果（API 端到端 10 项全过）
用户列表返回明文、建用户后可见、用户改密后同步、管理员重置后同步、重置后可正常登录、登录认证不受影响（22 项权限完整）。测试账号已清理。

⚠️ **安全提示**：明文密码存储降低了系统安全性，请确保 `backend/data/hr.db` 文件不外泄，且该功能仅限内网可信环境使用。

---

## 2026-07-16

### 通用层级文本解析器重构

#### 背景
替换原有 `ImportReportsModal.tsx` 和 `TaskTree.tsx` 中各自独立的解析逻辑，统一为可复用的 `task-parser.ts`，支持用户从外部任意粘贴的带层级文本。

#### 改动文件

| 文件 | 说明 |
|------|------|
| `src/components/weekly-report-v2/task-parser.ts` | **新增** 通用层级文本解析器 |
| `src/components/weekly-report-v2/TaskTree.tsx` | 移除原 `parseTaskHierarchy` / `parseCompactHierarchy` / `genId`，改为导入 `parseTaskHierarchy`；并将 `stripTaskPrefix` 改为内部函数，消除 React Fast Refresh HMR 警告 |
| `src/components/weekly-report-v2/ImportReportsModal.tsx` | 移除原 `detectImportedLevel` / `parseImportedTextToTree`，改为导入 `parseHierarchicalText` |
| `scripts/task-parser-test.mjs` | 改为直接导入解析器，并扩展到 49 个回归测试用例 |

#### 解析器能力

- 支持多种编号体系：
  - 阿拉伯数字：`1.` / `1、`
  - 中文数字：`一、` / `一.`
  - 字母：`a.` / `A.`
  - 罗马数字：`i.` / `I.` / `ii.`
  - 括号：`（1）` / `（一）` / `(a)`
  - 右括号：`1）` / `一）`
  - 带圈数字：`①`
  - Markdown 项目符号：`* ` / `- ` / `+ `
- 无视换行：可解析 `一、今天吃什么1.今天早饭吃鸡蛋2.中饭吃汉堡`
- 容忍空格/无空格：`1.abc` 与 `1. abc` 均可识别
- 避免小数误识别：`1.5` / `v1.2.3` / `2024.01.01` 不会被打散
- 避免 URL/邮箱误拆：`www.example.com` / `user.name@example.com` 保持完整
- 动态推断层级：按编号体系首次出现顺序自动建立层级，同体系自动保持同级
- 保留前言/标题文本：无序号的开篇文字会作为独立根节点保留
- 跳过空序号行：`1. ` 这类无内容行会被忽略
- 支持 Windows 换行符 `\r\n`

#### 验证结果

```bash
node scripts/task-parser-test.mjs
# ✅ 全部 49 个用例通过

npm run build
# ✅ TypeScript + Vite 构建通过
```
